# Notebooks

Place exploratory / demo notebooks here. The `scripts/infer.py` CLI already
covers the typical "run on a single image and visualize" workflow, but this
folder is a good home for ablation sweeps, hyperparameter experiments, and
qualitative comparisons across datasets.
